{% extends 'EGUserBundle::layout.html.twig' %}

{% block egBody %}
    <div class="container">
    {{ form(form) }}
    <a href="{{ path('eg_user_profile_edit', {id: user.id}) }}" class="btn btn-default "><span class="glyphicon glyphicon-chevron-left"></span> Retour</a></p>
    </div>
{% endblock %}